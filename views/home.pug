extends layouts/main
include mixins/videoBlock

block content    
    div.home-videos
        each video in videos
            +videoBlock({
                id: video.id,
                fileUrl:video.fileUrl,
                title:video.title,
                views:video.views
            })